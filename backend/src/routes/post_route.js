// import express from "express";
// export const postRouter = express.Router();
// import { getPost, addPost, deletePost } from "../controller/post_controller.js";
// import { uploadPost } from "../middlewares/multer.js";
// import multer from "multer";
// const upload = multer();

// postRouter.get("/get-post", (req, res) => getPost(req, res));
// postRouter.post("/add-post", upload.single("image"), (req, res) =>
//   addPost(req, res)
// );
// postRouter.delete("/delete-post/:postId", (req, res) => deletePost(req, res));
